 program factorial; var i,k: integer;
        function fact (n:integer):integer; 
        begin
            if n<=1 then fact:=1 else fact:=n*fact(n-1);
        end;
        BEGIN
            write ('ввoдcимвoлai'):readln(i); for k:=1 to i do writeln(fact(k));
        END.